PLP strip under the primary nav: breadcrumb left, hide-filters and sort controls right.

```jsx
<SubNavStrip breadcrumb={["Men", "Shoes", "Trail Running"]} sort="Featured" onToggleFilters={toggle} />
```

Canvas background with a 1px inset hairline bottom edge (`--elevation-2`) — the system's only sticky-bar depth cue.
