Hairline pill used for PLP filter and sort selections; flips to a solid ink fill when active.

```jsx
<FilterChip active>Trail</FilterChip>
<FilterChip count={42}>Road Running</FilterChip>
```

40px height, 16px horizontal padding, `--radius-pill`. Only two states exist: default (canvas + hairline) and active (ink + canvas text).
