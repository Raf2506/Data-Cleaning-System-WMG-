export interface BadgeProps {
  /** Promo copy — "Just In", "Coming Soon", "Recycled Materials", "Member Exclusive". */
  children?: React.ReactNode;
  style?: React.CSSProperties;
}
export function Badge(props: BadgeProps): JSX.Element;
