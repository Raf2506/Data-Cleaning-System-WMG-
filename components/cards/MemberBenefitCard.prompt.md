Photographic benefit tile used in the membership page's 3-up grid.

```jsx
<MemberBenefitCard headline="Member-only products" cta="Explore" />
```

Copy sits bottom-left: 24px heading in canvas plus a white on-image pill. Dark photography only — the heading is never inked.
