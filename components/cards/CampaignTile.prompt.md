The system's signature editorial unit — full-bleed photography with a towering uppercase lockup set into it.

```jsx
<CampaignTile eyebrow="New Arrivals" headline="Run Longer" cta="Shop" ratio="16/9" />
```

96px display type at line-height 0.9, zero radius, one white on-image pill bottom-left. Never place two campaign tiles at the same scale in one row, and never use this display tier for section headers.
